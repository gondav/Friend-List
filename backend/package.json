{
  "name": "backend",
  "version": "1.0.0",
  "description": "- Copy `.env.example` as `.env` then replace the settings in the `.env` file.\r - Use the following commands:\r   - `npm i` - install the dependencies\r   - `npm run dev` - start your app in dev mode with nodemon",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql2": "^2.3.3",
    "sequelize": "^6.24.0",
    "sequelize-typescript": "^2.1.3"
  },
  "devDependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.14",
    "@types/mysql": "^2.15.21",
    "@types/node": "^18.8.3",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
